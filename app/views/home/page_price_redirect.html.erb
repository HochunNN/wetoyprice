<h1>사이트로 이동중입니다 좀만 기다려주세용!!</h1>
<input type="hidden" id="redirect_url" value="<%= @redirect_url%>">
<script>
    var delay = 1500; //Your delay in milliseconds
    var URL = $("#redirect_url").val();
    
    setTimeout(function(){ window.location = URL; }, delay);
</script>